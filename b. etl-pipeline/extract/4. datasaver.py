class DataSaver(metaclass=Static):

        def save_missing(missing_rows):
            """Save missing_data to a CSV file"""
        
        def save_duplicates(duplicate_rows):
            """Save duplicates to a CSV file"""
